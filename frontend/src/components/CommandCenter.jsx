export default function CommandCenter({ command, setCommand, onSend, onListen, onToggleWake, listening, wakeEnabled, busy, voiceSupported }) {
  return <section className={`command-dock ${listening ? 'hearing' : ''}`}><button className={`voice-main ${listening ? 'active' : ''}`} onClick={onListen} disabled={!voiceSupported} aria-label="Listen for one voice command"><span>{listening ? '◼' : '●'}</span></button><div className="command-input"><label>{listening ? 'LISTENING NOW' : busy ? 'PROCESSING' : 'ASK JARVIS'}</label><input value={command} onChange={(event) => setCommand(event.target.value)} onKeyDown={(event) => event.key === 'Enter' && onSend()} placeholder={voiceSupported ? 'Type a command or tap the microphone…' : 'Type a command…'} aria-label="Jarvis command" /></div><button className={`wake-toggle ${wakeEnabled ? 'active' : ''}`} onClick={onToggleWake} disabled={!voiceSupported}><i />{wakeEnabled ? 'Always listening' : 'Wake word off'}</button><button className="execute" onClick={onSend} disabled={busy || !command.trim()}>{busy ? 'THINKING' : 'SEND'} <span>↗</span></button></section>
}
